# SoulSeek Organizer

```text
walk dload folder:
  * mp3 ? -> move : convert to mp3 & wav -> move

└── destination/
    └── album/
        └── mp3/
            └── *.mp3
        └── wav/
            └── *.wav
```

```**old, finished**```
